We use a png file for the project_span because compressing to webp actually increases the image size.

Additionally, webp files take more compute to decompress, so even if the webp file size was smaller, the additional compute would degrade performance.