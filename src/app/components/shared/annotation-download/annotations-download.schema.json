{
  "fields": [
    {
      "key": "timezone",
      "type": "timezone",
      "templateOptions": {
        "label": "Select timezone",
        "required": true,
        "validationMessages": [
          { "name": "required", "message": "Invalid or no timezone selected" }
        ]
      }
    }
  ]
}
