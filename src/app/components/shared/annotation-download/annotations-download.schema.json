{
  "fields": [
    {
      "key": "timezone",
      "type": "timezone",
      "props": {
        "label": "Select timezone",
        "required": true,
        "validationMessages": [{ "name": "required", "message": "Invalid or no timezone selected" }]
      }
    }
  ]
}
