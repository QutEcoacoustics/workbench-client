describe("UnsavedInputGuard", () => {
  it("should create", () => {
    pending();
  });

  it("should show a confirmation if the user tries to navigate away from the page with unsaved changes", () => {
    pending();
  });

  it("should not show a confirmation if the user tries to navigate away from the page without unsaved changes", () => {
    pending();
  });
});
