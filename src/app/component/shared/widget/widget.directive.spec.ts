import { WidgetDirective } from "./widget.directive";

describe("WidgetDirective", () => {
  it("should create an instance", () => {
    const directive = new WidgetDirective();
    expect(directive).toBeTruthy();
  });
});
